# Agent Instructions
- Do not add, modify, regenerate, or commit any PNG assets (including `interactions.png` and `interactions-long.png`) unless explicitly requested.
- Do not introduce PNG-related entries to `.gitignore`; image handling is managed elsewhere.
- Follow standard Go formatting conventions (use `gofmt`).
